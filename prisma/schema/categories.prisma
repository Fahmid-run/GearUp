model Categories {

  id String @id @default(uuid())
  category Category @default(fitness)
  gearitemId String @unique
  gearItems GearItems @relation(fields: [gearitemId], references: [id])

  @@map("categories")
}